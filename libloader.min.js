! function() {
    let script = document.createElement("script");
    if (script.onload = function() {
            window.dispatchEvent(new Event("aalloaded"), {
                bubbles: !0,
                cancelable: !0,
                composed: !1
            })
        }, document.head.appendChild(script), window.navigator.onLine) {
        let xmlhttp = new XMLHttpRequest;
        xmlhttp.open("get", "https://cdn.apewebapps.com/getver.php?dn=aal&cb=" + (new Date).getTime(), !0), xmlhttp.addEventListener("load", (function(e) {
            let curver = e.target.responseText;
            curver && curver.trim().length > 0 && (localStorage.setItem("libloader-ret", curver), script.src = "https://cdn.apewebapps.com/aal/" + curver + "/aal.min.js")
        })), xmlhttp.send()
    } else {
        const ret = localStorage.getItem("libloader-ret");
      	console.log("hahahahahahha");
        ret ? script.src = "https://cdn.apewebapps.com/aal/" + ret + "/aal.min.js" : console.log("navigator offline")
    }
}();
